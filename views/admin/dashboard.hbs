<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        :root {
            --white: #fff;
            --dark: #000000;

            --light-white: #F1F1F1;

            --ivory: #FFFFF0;

            --dark-gray: #333333;

            /* Vibrant Gradient Colors */
            --vibrant-gradient-top-left: #9564E0;
            --vibrant-gradient-top-right: #E1729C;
            --vibrant-gradient-bottom-left: #5781F5;
            --vibrant-gradient-bottom-right: #8A65FF;


            /* Box Shadow */
            --card-shadow: 4px 6px 15px rgba(0, 0, 0, 0.2);

            --glass-blur: 10px;

            --vibrant-gradient: linear-gradient(to bottom right,
                    var(--vibrant-gradient-top-left),
                    var(--vibrant-gradient-top-right) 33%,
                    var(--vibrant-gradient-bottom-left) 66%,
                    var(--vibrant-gradient-bottom-right));
        }

        body {
            height: 100vh;
            background: #202126;
        }

        :root {
            --white: #fff;
            --dark: #000000;

            --light-white: #F1F1F1;

            --ivory: #FFFFF0;

            --dark-gray: #333333;

            --yellowshade: #fefe6a;

            /* Vibrant Gradient Colors */
            --vibrant-gradient-top-left: #9564E0;
            --vibrant-gradient-top-right: #E1729C;
            --vibrant-gradient-bottom-left: #5781F5;
            --vibrant-gradient-bottom-right: #8A65FF;


            /* Box Shadow */
            --card-shadow: 4px 6px 15px rgba(0, 0, 0, 0.2);

            --glass-blur: 10px;

            --vibrant-gradient: linear-gradient(to bottom right,
                    var(--vibrant-gradient-top-left),
                    var(--vibrant-gradient-top-right) 33%,
                    var(--vibrant-gradient-bottom-left) 66%,
                    var(--vibrant-gradient-bottom-right));
        }

        .navbar {
            background: #f5f5f5;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            height: 70px;
            box-shadow: 0 6px 20px rgb(241, 239, 239);
        }

        .navbar-brand {
            color: var(--ivory);
        }

        .navbar-brand:hover {
            color: var(--white);
        }

        .navbar .nav-item {
            background: linear-gradient(135deg, #ebebeb 0%, #f4f4f7 100%);
            font-size: 13px;
            margin-right: 10px;
            margin-bottom: 5px;
            border-radius: 20px;
            border: none;
            padding: 2px 15px;

        }

        .navbar .logout {
            background: var(--white);
        }

        .navbar .nav-item:hover {
            background: var(--yellowshade);
        }

        .navbar .nav-item a {
            color: var(--dark);
        }

        /* General styling for the section */
        /* General styling for the section */
        /* General Styling */
        /* General Styling */

        .section-title {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--vibrant-gradient-bottom-right);
            border-bottom: 2px solid var(--vibrant-gradient-top-right);
            padding-bottom: 5px;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        h3,
        h4 {
            color: var(--white);
        }

        /* Card Styling */
        .card {
            background-color: #ffffff;
            border-radius: 8px;
            border: 1px solid #e0e0e0;
            height: 130px;
            /* Fixed height */
            padding: 15px;
            margin-bottom: 15px;
            box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.05);
            display: flex;
            flex-direction: column;
            justify-content: center;
            /* Center vertically */
            align-items: center;
            /* Center horizontally */
            text-align: center;
            /* Align text to center */
        }

        /* Text Styling */
        .card p {
            margin: 0;
            font-size: 1rem;
            color: #333;
            line-height: 1.5;
        }

        /* Highlighted Points in Larger Font on New Line */
        .card .name-group {

            font-size: 1rem;
            color: #333;
        }

        .card .points {
            font-size: 1.4rem;
            /* Larger font for points */
            font-weight: bold;
            color: #d9534f;
            /* Red accent for points */
            margin-top: 5px;
            /* Space between name and points */
        }

        @media (max-width: 768px) {
            .card .points {
                font-size: 1.6rem;
                /* Increase font size on smaller screens */
            }
        }

        /* Section Styling */
        .top-performers-group {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
        }

        .top-performers-group h3 {
            font-size: 1.8rem;
            color: #333;
            margin-bottom: 15px;
            text-align: center;
            font-weight: 600;
        }

        /* Group Name Styling */
        .top-performers-group ul {
            list-style-type: none;
            padding: 0;
        }

        .top-performers-group>ul>li {
            background-color: #ffffff;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 20px;
            box-shadow: 2px 2px 6px rgba(0, 0, 0, 0.05);
        }

        .top-performers-group>ul>li>strong {
            font-size: 1.2rem;
            color: #0d6efd;
            font-weight: 700;
        }

        /* Individual Performer Styling */
        .top-performers-group ul ul {
            list-style-type: none;
            padding: 10px 0 0 15px;
            margin: 0;
        }

        .top-performers-group ul ul li {
            background-color: #f7f7f7;
            border: 1px solid #dcdcdc;
            border-radius: 5px;
            padding: 10px;
            margin-bottom: 10px;
        }

        .top-performers-group ul ul li p {
            margin: 0;
            font-size: 1rem;
            color: #333;
        }

        /* Points Styling */
        .top-performers-group ul ul li p strong {
            font-size: 1.1rem;
            color: #d9534f;
            /* Red accent for points */
            font-weight: bold;
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow">
        <div class="container-fluid">
            <!-- Brand/logo -->
            <a class="navbar-brand" href="#">ALATHURPADI DARS FEST</a>

            <!-- Offcanvas toggler button (only visible on mobile) -->
            <button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="offcanvas"
                data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Navbar items for larger screens (hidden on mobile) -->
            <div class="collapse navbar-collapse d-none d-lg-block">
                <ul class="navbar-nav m-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a class="nav-link" href="admin/items/add">ADD ITEMS</a></li>
                    <li class="nav-item"><a class="nav-link" href="admin/items">GET ITEMS</a></li>
                    <li class="nav-item"><a class="nav-link" href="admin/contestants">MANAGE CONTESTANTS</a>
                    </li>
                    <li class="nav-item"><a class="nav-link" href="admin/create-jury">CREATE JURY PANEL</a></li>
                    <li class="nav-item"><a class="nav-link" href="admin/view-juries">VIEW JURIES</a></li>
                </ul>
                <ul class="navbar-nav m-auto mb-2 mb-lg-0">
                    <li class="nav-item logout"><a class="nav-link" href="admin/logout">Log Out</a></li>
                </ul>
                <ul class="navbar-nav m-auto mb-2 mb-lg-0">
                    <li class="nav-item logout"><!-- In your dashboard view, add the following button -->
                        <a href="admin/view-items-participants" class="btn">
                            View All Contestants
                        </a>
                    </li>
                </ul>
            </div>

            <!-- Offcanvas menu (only visible on mobile) -->
            <div class="offcanvas offcanvas-end d-lg-none" tabindex="-1" id="offcanvasNavbar"
                aria-labelledby="offcanvasNavbarLabel">
                <div class="offcanvas-header">
                    <h5 class="offcanvas-title" id="offcanvasNavbarLabel">Menu</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                </div>
                <div class="offcanvas-body">
                    <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
                        <li class="nav-item"><a class="nav-link" href="admin/items/add">ADD ITEMS</a></li>
                        <li class="nav-item"><a class="nav-link" href="admin/items">GET ITEMS</a></li>
                        <li class="nav-item"><a class="nav-link" href="admin/contestants">MANAGE CONTESTANTS</a>
                        </li>
                        <li class="nav-item"><a class="nav-link" href="admin/create-jury">CREATE JURY PANEL</a>
                        </li>
                        <li class="nav-item"><a class="nav-link" href="admin/view-juries">VIEW JURIES</a></li>
                    </ul>
                    <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
                        <li class="nav-item logout"><a class="nav-link" href="admin/logout">Log Out</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>
    <div class="container-fluid py-5">
        <div class="container">
            <section class="dashboard-section">
                <!-- Top Performers Section -->
                <div class="performers-section mb-5">
                    <h3>Top Performers</h3>
                    {{#if topPerformers}}
                    <div class="row">
                        <div class="col-md-4 col-sm-12 mb-3">
                            <h4 class="section-title">Subjunior</h4>
                            <ul class="list-unstyled">
                                {{#each topPerformers.subjunior}}
                                <li class="card">
                                    <p class="name-group">{{this.name}} - Group {{this.group}}</p>
                                    <p class="points">Points: {{this.totalPoints}}</p>
                                </li>

                                {{/each}}
                            </ul>
                        </div>
                        <div class="col-md-4 col-sm-12 mb-3">
                            <h4 class="section-title">Junior</h4>
                            <ul class="list-unstyled">
                                {{#each topPerformers.junior}}
                                <li class="card">
                                    <p class="name-group">{{this.name}} - Group {{this.group}}</p>
                                    <p class="points">Points: {{this.totalPoints}}</p>
                                </li>

                                {{/each}}
                            </ul>
                        </div>
                        <div class="col-md-4 col-sm-12 mb-3">
                            <h4 class="section-title">Senior</h4>
                            <ul class="list-unstyled">
                                {{#each topPerformers.senior}}
                                <li class="card">
                                    <p class="name-group">{{this.name}} - Group {{this.group}}</p>
                                    <p class="points">Points: {{this.totalPoints}}</p>
                                </li>

                                {{/each}}
                            </ul>
                        </div>
                    </div>
                    {{else}}
                    <p>No top performers found.</p>
                    {{/if}}
                </div>

                <!-- Group Points Section -->
                <div class="group-points-section mb-5">
                    <h3>Group Points</h3>
                    {{#if groupPoints}}
                    <div class="row">
                        <div class="col-md-4 col-sm-12 mb-3">
                            <h4 class="section-title">First</h4>
                            <ul class="list-unstyled">
                                {{#each groupPoints.First}}
                                <li class="card p-2 mb-2">
                                    <p class="name-group">{{this.groupname}}</p>
                                    <p class="points">{{this.totalPoints}}</p>
                                </li>
                                {{/each}}
                            </ul>
                        </div>
                        <div class="col-md-4 col-sm-12 mb-3">
                            <h4 class="section-title">Second</h4>
                            <ul class="list-unstyled">
                                {{#each groupPoints.Second}}
                                <li class="card p-2 mb-2">
                                    <p class="name-group">{{this.groupname}}</p>
                                    <p class="points">{{this.totalPoints}}</p>
                                </li>
                                {{/each}}
                            </ul>
                        </div>
                        <div class="col-md-4 col-sm-12 mb-3">
                            <h4 class="section-title">Third</h4>
                            <ul class="list-unstyled">
                                {{#each groupPoints.Third}}
                                <li class="card p-2 mb-2">
                                    <p class="name-group">{{this.groupname}}</p>
                                    <p class="points">{{this.totalPoints}}</p>
                                </li>
                                {{/each}}
                            </ul>
                        </div>
                    </div>
                    {{else}}
                    <p>No group points found.</p>
                    {{/if}}
                </div>

                <!-- Top Performers By Group Section -->
                <div class="top-performers-group">
                    <h3>Top Performers By Group</h3>
                    {{#if topPerformersByGroup}}
                    <ul>
                        {{#each topPerformersByGroup}}
                        <li>
                            <strong>{{this.groupname}}</strong>
                            <ul>
                                {{#each this.topPerformers}}
                                <li>
                                    <p>{{this.name}} - Points: {{this.totalPoints}}</p>
                                </li>
                                {{/each}}
                            </ul>
                        </li>
                        {{/each}}
                    </ul>
                    {{else}}
                    <p>No top performers by group found.</p>
                    {{/if}}
                </div>

            </section>
        </div>
    </div>


</body>

</html>